{
  "name": "matomo-db-schema",
  "private": true,
  "version": "0.0.1",
  "description": "",
  "scripts": {
    "build": "./build.sh",
    "preserve": "npm run build",
    "serve": "http-server ./schemaspy",
    "predeploy": "cp ./CNAME ./schemaspy",
    "deploy": "gh-pages -d ./schemaspy",
    "stop": "pkill http-server"
  },
  "devDependencies": {
    "gh-pages": "~2.1.1",
    "http-server": "~0.12.0"
  }
}
